# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml

# Virtual environments
.venv/
venv/
env/

# uv
# (uv.lock is committed; site-packages are not)

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Bridge runtime artefacts (live under ~/.openclaw/, but ignore if accidentally written here)
.openclaw/
telemetry.db
telemetry.db-journal
access.log
access.log.*
tokens.dev.json

# docs/openapi-v1.yaml is committed (generated, but kept in git so the
# typed brains_shared SDK ships with a known-good spec). The pre-commit
# hook re-runs `tools/dump-openapi.py` and fails the commit if it drifts.

# Claude Code local settings (per-machine; share .claude/settings.json instead)
.claude/settings.local.json
# Claude Code worktrees — local scratch dirs, never tracked.
.claude/worktrees/
